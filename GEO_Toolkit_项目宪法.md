# GEO Toolkit 项目宪法

> 这不是愿景文档,是**每天做决策的锚**。
> 每当冒出"要不要现在做 X"的念头,翻到第 4 节对照一下。
> 版本: v1 (够用版) · 最后更新: 2026-06-06

---

## 1. 这是什么 · 给谁

**一句话:** 给 SMB / 电商运营的 GEO(Generative Engine Optimization)自助工具——帮他们让自己的品牌更容易被 ChatGPT / Claude / Perplexity 等 AI 在回答里提到和推荐。

**核心用户:** 电商运营 / 中小企业主。**不是开发者**——他们看不懂 "web root"、"`<head>`",所以一切产出都要"运营也能照着做"。

**商业模式:** PLG 自助式(self-serve)。要的是很多很多小客户自己来用、自己付费,不是少数大 B2B 销售。

---

## 2. 北极星指标 ★

**30 天内拿到第一批付费(哪怕只有几单)。**

目的不是赚多少钱,是**验证"有人真的愿意为 GEO 付费"**这个根本假设。在这个验证完成前,产品做得再完整都是赌博。

判断一切的标准 → **见第 4 节**。

---

## 3. 三个 MVP 的角色与定位

三个工具是一条完整工作流:**诊断网站 → 诊断 AI 现状 → 生产修复资料**。

| 工具 | 回答什么问题 | 产出性质 | 免费给什么 | 收费解锁 | 收费模式 |
|---|---|---|---|---|---|
| **A · GEO Audit** | 我的网站对 AI 友好吗?哪里要改? | 一次性消费(体检) | 总分+8维度分+AI怎么看你 | 完整 fix + Shopify落地指引 + PDF | **一次性 $9.90** ✅已上线 |
| **B · Prompt Engine** | 用户问 AI 时提到我了吗?机会在哪? | **持续监测**(AI在变) | SOV% + 认知评级 + 5类强弱 | 20prompt明细 + 竞品差距 + 抢回策略 | 待定(监测型→适合订阅) |
| **C · Asset Generator** | 帮我把要改的内容生成出来 | 一次性消费(生产) | 待定(预览/1个文件) | 完整 asset 包下载 | 待定(一次性) |

**关键认知:** A 和 C 是"做一次的事"(偏一次性),B 是"持续看变化的事"(偏订阅)。不要强行统一收费模式。

**当前状态提醒:**
- A 已上线收费,功能跑通(8/8 维度 fix,中英双语,PDF 无 tofu)
- B/C 功能完成度未知,需体检。它们原来唯一的收费入口($29.90/月订阅)目前被隐藏(`PRO_MONTHLY_ENABLED=False`)

---

## 4. 当前阶段的唯一优先级 ★★★

> **让 MVP-A 能被目标用户看到,并产生第一批付费。**

**决策法则:** 任何想做的事,先问一句——

> 「这件事**直接**帮我更快拿到第一笔付费吗?」

- **是** → 现在做
- **否** → 写进第 5 节 Backlog,**不要立刻做**

### 为什么是 A,不是 B/C
A 已经能收钱了。先用 A 赚到第一批钱、验证有人愿付,**再**决定投不投入 B/C。
- 若 A 没人付费 → 补 B/C 是浪费
- 若 A 有人付费 → 那才是 B/C 值得做的信号

**先把一个工具卖出去,胜过把三个工具做完整。**

---

## 5. Backlog(该做,但现在不做)

写在这里 = 不会忘,也不用怕错过,更不用急着插队。等北极星达成或优先级变化再取出来。

- [ ] **推广路径**(开局想做但被工程挤掉了——其实这个最该优先,见第6节)
- [ ] MVP-B 功能体检 + 补全 + 收费墙设计
- [ ] MVP-C 功能体检 + 补全 + 收费墙设计
- [ ] B 的订阅模式(需改 `verify_unlock` 支持 subscription)
- [ ] Shopify 插件(已调研:轻量引流型优先,先注册 Partner 占位)
- [ ] 防止同邮箱重复白嫖免费 audit(规模化后再说)
- [ ] 日语 / 西班牙语(有中英付费证据后再加)
- [ ] 验证 Supabase emails 表真有数据(确认邮箱没在丢)
- [ ] `<head>` 在 PDF 里被吞字的小 bug

---

## 6. 一个诚实的自我提醒

开局(2026-06-06)定的两件事是「① 收费+推广 ② Shopify 插件」。
一天下来,收费做完了,但**推广一直没碰**,精力全进了功能打磨(where_to_apply、多语言、tofu)。

每一步都"合理",但合起来偏离了"赚到第一批钱"。
**功能够用就行,赚钱靠的是让人看到、让人用。**
下次想优化功能前,先问:产品已经发出去给人用了吗?

---

## 7. 已确立的技术原则(血泪教训,别再犯)

- **付费解锁必须经过 `verify_unlock`(Stripe session_id 反查),绝不留 `?paid=true` 裸后门。** 信金额(990/2990),不信 URL 参数。
- **所有 LLM 调用必须设 timeout(60s)**,否则会无限挂起拖死整个流程。超时异常要可重试。
- **LLM 中文输出:禁 emoji、禁 ✓✗ 等符号(PDF 的 CJK 字体渲染成 tofu);引号用「」,禁裸 ASCII `"`(撑破 JSON)。**
- **PDF 渲染中文:所有正文样式用 CJK 字体,且 `registerFontFamily` 把 bold/italic 映射回 regular**(否则粗体变体会偷偷退回 Helvetica → tofu)。
- 改动分阶段、本地测试 → 审查 → 再 push。不留半成品后门。
- 非核心功能用 flag 干净地隐藏(如 `PRO_MONTHLY_ENABLED`),不留半实现。
- 成本守卫:单次 audit ~$0.09–0.13,警戒线 $0.30。

---

## 8. 单位经济(健康度参考)

- 单次 audit 成本: ~$0.09(英文)/ ~$0.13(中文,LLM 输出更长)
- 售价: $9.90 → 成本占比约 1–1.3%,毛利健康 ✅
- 结论: 商业模式在单位经济上成立。瓶颈不在成本,在**有没有人来付费**(回到北极星)。
