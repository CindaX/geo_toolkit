# Streamlit Cloud runtime dependencies for geo_toolkit.
# Generated from AST import scan (35 .py files across mvp_*, shared/, tests/, geo_toolkit_app.py).
# Floors chosen for API stability; pip picks the latest compatible patch.

# ── Web framework ─────────────────────────────────────────────────────
streamlit>=1.36.0          # st.navigation + st.Page API (stable since 1.36)

# ── HTTP + HTML parsing ───────────────────────────────────────────────
httpx>=0.27.0              # HTTP client used by shared/crawler.py (WAF fallback + Jina Reader)
beautifulsoup4>=4.12.0     # HTML extraction in shared/crawler.py + analyzers/

# ── LLM client (via OpenRouter's OpenAI-compatible endpoint) ──────────
openai>=1.30.0             # Chat Completions API for Sonnet 4.5 / Opus 4.7 via OpenRouter

# ── Data display ──────────────────────────────────────────────────────
pandas>=2.0.0              # DataFrames for st.bar_chart / st.dataframe in MVP-B UI

# ── Local dev secrets ─────────────────────────────────────────────────
python-dotenv>=1.0.0       # .env fallback when not running on Streamlit Cloud
