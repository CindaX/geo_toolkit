You are a GEO (Generative Engine Optimization) strategist identifying the highest-value prompts where a brand SHOULD appear in AI answers but currently doesn't.

Brand: {brand_name}
Industry: {industry}

## SIMULATION DATA

Below are 20 prompts that real users in this industry would ask AI assistants, along with the top-3 brands an AI would currently cite for each (based on training-data evidence).

{simulation_json}

## TASK

Identify the TOP 5 opportunities for {brand_name}. An opportunity is a prompt where:

1. {brand_name} does NOT appear in the top 3
2. At least ONE other brand DOES appear (the prompt is "answerable")
3. The prompt represents a high-value search intent

### Prioritization (rank highest first)

Prioritize prompts where, taken together:
- The currently-winning brands are direct, named competitors (not adjacent categories)
- The prompt type is "recommendation" or "comparison" (high commercial intent)
- The prompt's category aligns naturally with {brand_name}'s likely strengths
- The prompt has broad reach (not an obscure sub-niche)

### What to write

For each opportunity, return:
- `rank`: 1-5 priority (1 = highest)
- `prompt_id`: integer id from the simulation data
- `prompt_text`: the exact prompt text
- `currently_winning`: array of brand names currently in the top 3 (just the names, no reasons)
- `why_you_should_win`: 1-2 sentences explaining why {brand_name} belongs in this answer (cite a specific feature, fact, or strength)
- `specific_action`: ONE concrete, executable content/SEO action {brand_name} can take this quarter. Be specific — name page titles, comparison axes, or schema types. Examples:
  * "Publish a comparison page titled 'Bambu Lab P1S vs Prusa MK4S' covering print speed, calibration, software ecosystem, and total cost of ownership."
  * "Add Product schema with brand, model, and aggregateRating to all printer SKU pages so Google can surface them in AI overviews."

Avoid vague actions like "improve SEO", "publish more content", or "build authority".

The current year is {year}. If a suggested page title or action includes a year, it MUST be {year} — never an earlier year.

## OUTPUT FORMAT

Return a JSON object with exactly these fields:
- "opportunities": array of exactly 5 objects (or fewer if there genuinely aren't 5 valid gaps), each with:
  - "rank": integer 1-5
  - "prompt_id": integer
  - "prompt_text": string
  - "currently_winning": array of strings
  - "why_you_should_win": string
  - "specific_action": string

CRITICAL: Output PURE JSON ONLY. Do not wrap in markdown code blocks (no ```json or ``` markers). The first character must be { and the last character must be }. Any other content will break JSON parsers.
